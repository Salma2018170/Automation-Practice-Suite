name: workflow name

on:
  push:
    branches:
      - master
jobs:
  example-job:
    runs-on: Ubuntu-latest

    steps: 
      - name: print welcome message
        run: echo "Welcome to ur first push."
        
    
